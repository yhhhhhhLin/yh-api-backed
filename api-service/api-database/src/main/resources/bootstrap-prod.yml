spring:
  application:
    name: database-service
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848
        file-extension: yml
        namespace: yhapi-default
server:
  port: 7777
